{
  "name": "buywise-api",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker proxying the eBay Browse API for BuyWise. Holds the eBay credentials so the static frontend never sees them.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260827.1",
    "typescript": "^5",
    "wrangler": "^4.127.0"
  }
}
